INSTALE AS DEPENDÊNCIAS DO PROJETO:
        npm init -y
        npm install express node-fetch uuid ejs
        npm install express
        npm install node-fetch@2
        npm install uuid
        npm install ejs
